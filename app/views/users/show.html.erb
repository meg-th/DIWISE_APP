<div class = "container">
  <div class = "row">
    <div class="col-5">
      <h1>Welcome back, <%= @user.username %></h1>
    </div>
    <div class="col-7">
      <% if @user.projects.empty? %>
        <h2>You don’t have any projects!</h2>
      <% else %>
        <h2>Your Projects</h2>
      <% end %>
      <% @user.projects.each do |project| %>
        <ul class="">
          <li><h3 class=""><%= link_to project.title, project_path(project) %></h3></li>
        </ul>
      <% end %>
      <a class= "mx-4"><%= link_to "Add a New Project", new_project_path, class: "btn btn-success" %></a>
    </div>
  </div>
</div>


